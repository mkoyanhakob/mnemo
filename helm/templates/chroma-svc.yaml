apiVersion: v1
kind: Service
metadata:
  name: chromadb-service
spec:
  clusterIP: None
  selector:
    app: chromadb
  ports:
  - port: {{ .Values.chromadb.service.port }}
    targetPort: {{ .Values.chromadb.service.port }}